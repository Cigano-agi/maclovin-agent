version: '3.8'

services:
  maclovin-agent:
    build: .
    container_name: maclovin-agent
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./briefings:/app/briefings
      - ./data:/app/data
    command: ["uv", "run", "python", "-m", "maclovin", "run"]
